/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fakestoreapi.com'],
      },
      rules: {
        'rule-name': 'off', 
      
    },
};

export default nextConfig;
